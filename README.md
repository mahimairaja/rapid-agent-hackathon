# rapid-agent-hackathon

A reusable FastAPI backend template: clean layering, dependency injection,
async SQLModel/PostgreSQL, JWT auth, and an MCP tool surface. Ships with a
single `User` CRUD domain as the pattern to copy.

See [`backend/README.md`](backend/README.md) for setup and usage.