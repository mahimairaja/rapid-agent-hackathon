# Baked into `vite build` (Vercel). Points the hosted frontend at the
# Cloud Run backend; without this the bundle falls back to localhost and the
# hosted demo cannot reach the API. Contains no secrets.
VITE_API_BASE_URL=https://rapid-agent-hackathon-dxiegyhtya-pd.a.run.app/api/v1
